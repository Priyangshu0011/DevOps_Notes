Task:

1) Inside this same running container, you are required to.
2) Create a new directory named /project
3) Inside /project, create a file called report.txt
4) Add the following text inside the file:
    This is our container interaction with our host machine.
5) Verify the content.
6) Copy report.txt from the container to your Desktop.
7) Create another file named notes.txt in the same /project directory.
8) Append the text:
9)Verify that both files exist inside /project.

Practice Questions:

1) You need to start a new container using the nginx image while setting an environment variable
ENV_MODE=production. Write the docker run command to achieve this.
2) You have a running container named my_app, but it's not behaving as expected. You want to check its logs to debug any errors. Write the command to view its logs and follow new log entries in real-time.
3)A container named web_server was stopped. Write the command to: 
    a)Start the container again. 
    b)Stop the container when needed. 